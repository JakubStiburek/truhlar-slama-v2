<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
  <link rel="manifest" href="/site.webmanifest" />
  <title>Truhlář Sláma</title>
  <meta name="viewport" content="width=device-width" />
  <meta name="description"
        content="Truhlář Ondřej Sláma. Řemeslná výroba ze dřeva - nábytek, zakázková výroba, unikátní kousky. Tel: 608334824, mail: ondrej.slama@truhlarslama.cz" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link rel="stylesheet" type="text/css" href="css/index.css" />
  <link rel="stylesheet" type="text/css" href="css/portfolio.css" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&family=Roboto:ital,wght@0,400;0,700;1,400&display=swap"
    rel="stylesheet">
</head>
<body>
{{> header}}
<div class="page-control">
  <a class="page-button" href="/portfolio?page=1"><<</a>
  {{#each pagination.pages}}
    {{#if this.current}}
      <a class="page-button pagination-current" href="/portfolio?page={{this.number}}">{{this.number}}</a>
    {{else}}
      <a class="page-button" href="/portfolio?page={{this.number}}">{{this.number}}</a>
    {{/if}}
  {{/each}}
  <a class="page-button" href="/portfolio?page={{pagination.total}}">>></a>
</div>
<div class="assets-row">
  <div class="assets-column">
    {{#each assetsLeft}}
      <div class="assets-item">
        <img src="{{this.secure_url}}" alt="{{this.context.caption}}" class="image">
        {{#if this.context.caption}}
          <div class="overlay">{{this.context.caption}}</div>
        {{/if}}
      </div>
    {{/each}}
  </div>
  <div class="assets-column">
    {{#each assetsRight}}
      <div class="assets-item">
        <img src="{{this.secure_url}}" alt="{{this.context.caption}}" class="image">
        {{#if this.context.caption}}
          <div class="overlay">{{this.context.caption}}</div>
        {{/if}}
      </div>
    {{/each}}
  </div>
</div>
<div class="page-control">
  <a class="page-button" href="/portfolio?page=1"><<</a>
  {{#each pagination.pages}}
    {{#if this.current}}
      <a class="page-button pagination-current" href="/portfolio?page={{this.number}}">{{this.number}}</a>
    {{else}}
      <a class="page-button" href="/portfolio?page={{this.number}}">{{this.number}}</a>
    {{/if}}
  {{/each}}
  <a class="page-button" href="/portfolio?page={{pagination.total}}">>></a>
</div>
{{> footer}}
</body>
</html>
